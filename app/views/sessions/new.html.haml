%h1 Sign In

- if flash[:error]
  %p
    = flash[:error]

= simple_form_for(:session, url: sessions_path) do |f|
  = f.input :email
  = f.input :password
  = f.button :submit, value: 'Sign In', class: 'btn btn-primary'